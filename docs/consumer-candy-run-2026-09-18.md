# Consumer 糖果题实测

2026-09-18。一次性测试 Consumer → Relay3 → Provider4 → gpt-5.5，经 V9 受控测试网执行。成功请求耗时 46.533 秒，HTTP 200；Provider 响应签名与付款绑定验证通过。

## Provider 原始回答

> 主句答案：最少21个，可摸形状取9个圆形、12个五角星。若仍不满足，只能落在几类“坏组合”，其圆/星上限为(24,4)(17,10)(15,11)(8,17)，均容不下(9,12)，故保证。括号有笔误，应为“星苹果+圆桃子”；按括号字面则最少23个：18圆、5星。

独立复核一致：按主句取 9 圆形、12 五角星，共 21 颗。9 圆形必有苹果或桃子；12 五角星必同时有苹果和桃子。下界：假设每种形状都按西瓜→苹果→桃子出现，则形成异形异味配对至少需要 9 圆+12 星或16 圆+5 星，均为21，前20不能保证。括号字面对应另一条件，答案23（18圆+5星）。

## 请求与结算

- 请求 ID：`0x82581c73c3afd55398085fdbab4c8fe45e357071caeb08ff89dcebe4f615abe4`。
- Provider signer：`0xf3217abadf55b970fd029cc17beabc1cc12b099b`。
- 结算交易：`0xd3e5b3c98d9fc53e4bd35c0c8b50654cc89bf7ea16198b9ffde3a21429b095b4`，区块 11728592；核验快照已有 8 个区块确认。
- 实际费用：12690 最小单位，即 0.012690 测试稳定币。
- 核验时状态：`pending`，表示费用已托管、处于争议窗口，尚未释放或领取；不是交易待上链。
- 输出预算2000 tokens，reasoning effort=low；实测输入6970、输出1430（包含推理用量）。

## 失败记录与限制

初次设置3000输出上限，被当前Provider的2000预留上限在推理前拒绝（HTTP400）。第二次2000上限请求已执行，但实际输出含推理2355，执行后校验拒绝返回（HTTP422）。两次都没有付款回执；区块 11728599 的链上核验均为无结算记录。第三次改为简短答案和低推理强度，重新签署新请求后成功；未重放原请求。

当前后端输出上限是执行后校验，低推理强度不能保证所有任务都不超限。较长任务需协调Provider、网络清单及实际侧车额度并覆盖签名费用上限，不能关闭计量校验。本次没有改变在线节点配置。

证据位于 `.codex-run/mesh/v9-controlled-test-20260918/consumer-runs/` 的三个 `candy-20260918*` 目录；成功目录保存 `verified.json`、`receipt.json`、`settlement-verified.json` 与 `provider-answer.txt`。不在报告中公开付款签名、钱包私钥或原始签名交易。
